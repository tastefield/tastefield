## Meta

- Section: 02 What's compiled today
- Plan heading: What's compiled today — *and what's coming*
- Status: drafted · hidden from live site (roadmap still fuzzy)
- Headline pick: This is what works today, and what we will build next. (live draft). Plan/rec alts listed below; not overwritten.
- Skills used: copywriting → copy-editing → reviews → deslop; 2026-08-18 reframe adds Option A–C headlines only
- Filler check: PASS (re-run after review fixes)

## User-facing copy

### Eyebrow

Shipped · Soon · Next

### Headline (live)

This is what works today, and what we will build next.

### Subhead / body

Shipped items and planned items share one list. A badge shows how far each item is.

**Shipped.** CSS custom properties and Tailwind `@theme`. Component contracts (`cva` variants). Nine compiled rules: `no-marketing-filler` (voice), `empty-states-need-action` (empty states), `no-raw-hex`, `no-arbitrary-values`, `no-arbitrary-color-utility`, `no-inline-border-radius`, `no-invented-variants`, `no-restyling-primitives`, `prefer-design-system-components`.

**Soon.** Remaining Copy and Brand coverage. Motion as a documented Foundation. Figma and zeroheight as source connections.

**Next.** A public registry. A playbook for each role. A dual-axis leaderboard. A marketplace.

### New headline options (2026-08-18)

**Option A (rec).** Today, and what we're building toward.

**Option B.** Built. Soon. On the horizon.

**Option C.** Status, without the pitch.

### CTAs

(none)

### Trust / badge lines

`Soon` sits on the same list. `Next` sits in a separate block.

## Alternates (preserved)

- What's compiled today. — *plain*
- Built, soon, and on the horizon. — *structural*

## Visual notes

Shipped items unbadged. `Soon` badges inline. Separate "On the horizon" block carrying `Next`. Not an apology strip under the H1. Matches Blume-style honesty tiers.

## Review notes

### Skeptical developer

- Named all nine rule IDs; do not collapse them into a paraphrase list in UI if the list is the proof.

### Honesty auditor

- Copy stays in Shipped (narrow). Remaining Copy/Brand is Soon.
- Registry / recipes / leaderboard / marketplace stay Next (Type research: do not lead Stage 1 with marketplace).

### Rhythm

- Opener couplet kept on live headline. New options are shorter frames for when this section goes live.

## Rule gaps

- Badging is the control for unbuilt work, not the filler regex.

## Notes

Plan correction encoded: Copy is shipped-narrow, not unbuilt. zeroheight named only as a Soon source connection.
