## Meta

- Section: 04 Advisory vs. verified
- Plan heading: Advisory vs. verified
- Status: drafted · no section on the site yet (promote earlier in scroll: after Problem)
- Headline pick: plan recommended thesis couplet (this is the one section that states it)
- Skills used: copywriting → copy-editing → writing-guidelines → reviews → deslop; 2026-08-18 reframe adds Option B/C against zeroheight-style "context MCP"
- Filler check: PASS (manual)

## User-facing copy

### Eyebrow

Advice and enforcement

### Headline (live pick)

A rule your agent can read is advice. A rule that can fail is enforcement.

### Subhead / body (live)

A markdown file of brand guidelines sits in the model's context window with everything else. The model can weigh it or skip it. A compiled rule carries a checkable pattern. When the agent breaks the rule, a check fails and shows the line.

### New options (2026-08-18)

**Option A (rec):** keep live headline + body.

**Option B.** Headline: Context is a suggestion. Constraints are not. Body: Most MCP tools load more input. Tastefield checks the output against rules compiled from what you already shipped.

**Option C.** Headline: Everyone loads context. We give your agent something it can fail. Body: Serving guidelines into the editor is useful. Verifying the result against your tokens and contracts is the half that sticks.

### CTAs

(none)

### Trust / badge lines

(none)

## Alternates (preserved)

- Context is a suggestion. Constraints are not. (*plan couplet alt; denser antithesis*)
- Everyone gives your agent context. We give it something it can fail. (*plan competitive; "everyone" is an intensifier*)
- Advice doesn't have a line number. (*plan compressed*)
- The difference between telling and checking. (*plan plain; safest against FORBIDDEN isocolon*)

## Visual notes

Two cards. Left "Advisory": prose in a `.mdc`, muted, no outcome. Right "Verified": the same intent as a compiled rule with its pattern, and a real result beneath (hex and line number live in the demo, not in the body copy).

## Review notes

### Skeptical developer

- "A rule that can fail" is true only when a check runs. Body already says "a check fails," not "the write is blocked automatically."

### Honesty auditor

- Matches the locked category definition in `.agents/product-marketing.md`.
- Differentiates from zeroheight-style context serving without naming them in the H1 (FAQ carries the named comparison).

### Rhythm

- Headline is the one allowed thesis couplet for the page.
- Body is short sentences, active voice, no second antithesis stack.

## Rule gaps

Headline is an isocolon couplet. FORBIDDEN bans that pattern for general section prose; this section is the one place the thesis is allowed to appear as a couplet. If a later pass rejects the couplet, promote "The difference between telling and checking."

## Notes

This section should sit immediately after Problem in the reframed page order.
