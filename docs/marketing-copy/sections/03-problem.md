## Meta

- Section: 03 The problem
- Plan heading: The problem
- Status: drafted
- Headline pick: Off-brand work can still pass review. (user lock)
- Filler check: PASS
- Reframe 2026-08-18: Option A–C added; user lock kept as primary. Invent/drift language borrowed from zeroheight problem framing without cloning "from / not around."

## User-facing copy

### Eyebrow

The problem

### Headline (locked)

Off-brand work can still pass review.

### Subhead / body (live)

The agent fills the gap with generic-looking UI. AI coding tools do not continually sync with your design system, Figma file, or stylesheet.

### New options (2026-08-18)

**Option A (rec body alt beside locked headline).** Agents invent colors, props, and tone your system already ruled out. The PR still looks finished.

**Option B.** Headline: Agents without your rules invent their own. Body: More agent output means more drift. Context helps. Unchecked output still ships.

**Option C.** Headline: Plausible is the failure mode. Body: Broken code is easy to reject. Generic UI that matches nothing in your system still looks fine in a PR.

### CTAs

(none)

### Trust / badge lines

(none)

## Alternates (preserved)

- An agent with no constraints will always produce something plausible. — *plan recommended; kept as alt; "plausible" is the hard word*
- Nobody catches this in review. That's the problem.

## Visual notes

Do not typeset the old left/right null-answer stack (Figma / `globals.css` / `#3B82F6` / seventh variant / "cheap"). That was the readability problem.

Use the existing ViolationWipe under this copy: the demo names the misses. The prose stays a story.

## Review notes

### Skeptical developer

Rerun dropped fake-specifics (hex, variant count, banned word) that read as a puzzle. Still a constructed scene, not a measured incident.

### Honesty auditor

No product claim, no CTA, no unbadged shipping. Does not claim Tastefield reads PDFs or Figma.

### Rhythm

Locked headline stays. New options are available if invent/drift should lead.

## Rule gaps

Filler regex still wouldn't catch a hex/variant stack. Readability is a human pass.

## Notes

User: the skilltune stack made the section hard to understand. Reran in prose. Plan stack kept only as a rejected mechanic, not as live copy.
